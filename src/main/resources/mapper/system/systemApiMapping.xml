<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yibo.parking.dao.system.SystemApiMapper">

    <select id="findList" resultType="SystemApi">
        select
            a.id as "id",
            a.name as "name",
            a.api_url as "apiUrl",
            a.paramters as "paramters"
        from system_api a
        where 1 = 1
        <if test="name != null and name != ''">
            and name = #{name}
        </if>
    </select>

    <insert id="insert">
        insert into system_api(
            id,
            name,
            api_url,
            paramters
        )
        values (
            #{id},
            #{name},
            #{apiUlr},
            #{paramters}
        )
    </insert>

    <update id="update">
        update system_api set
            name = #{name},
            api_url = #{apiUrl},
            paramters = #{paramters}
        where id = #{id}
    </update>

    <delete id="del">
        delete from system_api where id = #{id}
    </delete>
</mapper>