<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yibo.parking.dao.car.DeviceMapper">

    <select id="findList" resultType="Device">
        select
            a.id as id,
            a.device_id as deviceId,
            a.car_id as carId,
            a.status as status
        from device a
        where 1 = 1
        <if test="deviceId != null and deviceId != ''">
            and device_id = #{deviceId}
        </if>
        <if test="carId != null and carId != ''">
            and car_id = #{carId}
        </if>
    </select>

    <select id="get" resultType="Device">
        select
            a.id as id,
            a.device_id as deviceId,
            a.car_id as carId,
            a.status as status
        from device a
        where 1=1
        <if test="id != null and id != ''">
            and a.id = #{id}
        </if>
    </select>

    <insert id="insert">
        insert into device(
            id,
            device_id,
            car_id,
            status
        )
        values (
            #{id},
            #{deviceId},
            #{carId},
            #{status}
        )
    </insert>

    <update id="update">
        update device set
            device_id = #{deviceId},
            car_id = #{carId},
            status = #{status}
        where id = #{id}
    </update>

    <update id="stop">
        update device set
            status = '-1'
        where id = #{id}
    </update>

    <update id="start">
        update device set
            status = '0'
        where id = #{id}
    </update>

    <delete id="del">
        delete from device where id = #{id}
    </delete>
</mapper>